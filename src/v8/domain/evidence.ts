import {immutable,invariant,requireKnown} from "../constitution/invariants.js";import {evidenceId,sourceId,type EvidenceId,type SourceId} from "./primitives.js";
export type EvidenceIngestionState="INGESTED"|"AUDITED"|"REJECTED"|"UNKNOWN";export interface Evidence{id:EvidenceId;sourceId:SourceId;locator:string;excerpt:string;ingestion:Exclude<EvidenceIngestionState,"UNKNOWN">;capturedAt:string;}
export function createEvidence(i:Omit<Evidence,"id">&{id?:string}):Readonly<Evidence>{const ingestion=requireKnown(i.ingestion,"V8_EVIDENCE_UNKNOWN","evidence.ingestion");invariant(i.excerpt.trim().length>0,"V8_EVIDENCE_EMPTY","Evidence excerpt cannot be empty.");return immutable({...i,id:evidenceId(i.id??`${i.sourceId}:${i.locator}:${i.capturedAt}`),sourceId:sourceId(i.sourceId),ingestion});}
