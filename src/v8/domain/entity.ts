import {immutable} from "../constitution/invariants.js";import {entityId,nonEmpty,type EntityId} from "./primitives.js";export interface Entity{id:EntityId;type:string;canonicalName:string;}export function createEntity(i:Omit<Entity,"id">&{id?:string}):Readonly<Entity>{return immutable({id:entityId(i.id??`${i.type}:${i.canonicalName}`),type:nonEmpty(i.type,"entity.type"),canonicalName:nonEmpty(i.canonicalName,"entity.canonicalName")});}
