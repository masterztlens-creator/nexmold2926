import {immutable,invariant,requireKnown} from "../constitution/invariants.js";import {claimId,knowledgeId,nonEmpty,type ClaimId,type Fingerprint,type KnowledgeId} from "./primitives.js";import {contentFingerprint} from "../foundation/hash.js";
export type KnowledgeStatus="APPROVED"|"REJECTED"|"UNKNOWN";export interface Knowledge{id:KnowledgeId;proposition:string;claimIds:readonly ClaimId[];status:Exclude<KnowledgeStatus,"UNKNOWN">;fingerprint:Fingerprint;}
export function createKnowledge(i:Omit<Knowledge,"id"|"fingerprint">&{id?:string}):Readonly<Knowledge>{const status=requireKnown(i.status,"V8_KNOWLEDGE_UNKNOWN","knowledge.status");invariant(i.claimIds.length>0,"V8_KNOWLEDGE_NO_CLAIM","Knowledge must derive from at least one claim.");const claimIds=immutable([...new Set(i.claimIds.map(claimId))].sort());const proposition=nonEmpty(i.proposition,"knowledge.proposition");const fp=contentFingerprint({proposition,claimIds,status});return immutable({id:knowledgeId(i.id??`knowledge:${fp}`),proposition,claimIds,status,fingerprint:fp});}
