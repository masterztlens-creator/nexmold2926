import type {FoundationRecord,LineageLink} from "../foundation/types.js";export interface EligibilityInput{readonly subjectId:string;readonly evidenceIds:readonly string[];readonly ruleIds:readonly string[];readonly scopeId?:string;}export interface EligibilityResult{readonly eligible:boolean;readonly subjectId:string;readonly reasons:readonly string[];readonly lineage:readonly LineageLink[];readonly record?:FoundationRecord;}
