import {immutable,invariant} from "../constitution/invariants.js";import type {FoundationStore} from "../foundation/types.js";import {createPublicationArtifact} from "./artifact.js";import type {PublicationInput} from "./types.js";export class PublicationGate{constructor(private readonly store:FoundationStore){}publish(i:PublicationInput){const e=this.store.get("ELIGIBILITY",i.eligibility.aggregateId,i.eligibility.version);invariant(e!==null&&e.fingerprint===i.eligibility.fingerprint&&e.state==="APPROVED","V8_PUBLICATION_STALE_ELIGIBILITY","Eligibility record is stale or not approved.");const policy=this.store.get("POLICY",i.policyId);invariant(policy!==null&&policy.state==="APPROVED","V8_PUBLICATION_POLICY_NOT_APPROVED","Publication requires an APPROVED policy.");const p=createPublicationArtifact(i);return immutable(p);}}
