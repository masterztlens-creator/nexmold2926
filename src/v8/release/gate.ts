import {immutable,invariant} from "../constitution/invariants.js";import type {ReleaseArtifact} from "./types.js";export function assertReleaseReady(r:ReleaseArtifact){invariant(r.manifest.length>0,"V8_RELEASE_EMPTY_MANIFEST","Release manifest cannot be empty.");invariant(r.fingerprint.length===64,"V8_RELEASE_FINGERPRINT_INVALID","Release fingerprint must be SHA-256.");return immutable({passed:true,releaseId:r.id});}
