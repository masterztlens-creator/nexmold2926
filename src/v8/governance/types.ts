import type {AuditActor,FoundationRecord} from "../foundation/types.js";import type {Claim} from "../domain/claim.js";import type {Knowledge} from "../domain/knowledge.js";
export interface GovernanceContext{readonly actor:AuditActor;readonly reason:string;}export interface ClaimVerificationResult{readonly verdict:"VERIFIED"|"REJECTED"|"REQUIRES_REVIEW";readonly claim:Readonly<Claim>;readonly evidenceCount:number;readonly reasons:readonly string[];}export interface KnowledgeApprovalResult{readonly verdict:"APPROVED"|"REJECTED"|"REQUIRES_REVIEW";readonly knowledge:Readonly<Knowledge>;readonly verifiedClaimCount:number;readonly reasons:readonly string[];readonly record?:FoundationRecord;}
