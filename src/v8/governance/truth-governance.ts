import {immutable,invariant} from "../constitution/invariants.js";import type {Claim} from "../domain/claim.js";import type {Knowledge} from "../domain/knowledge.js";import {FoundationService} from "../foundation/service.js";import {ClaimVerifier} from "./claim-verifier.js";import {KnowledgeGovernance} from "./knowledge-governance.js";import type {GovernanceContext,ClaimVerificationResult,KnowledgeApprovalResult} from "./types.js";
export class TruthGovernance{readonly claims:ClaimVerifier;readonly knowledge:KnowledgeGovernance;constructor(readonly foundation:FoundationService){this.claims=new ClaimVerifier(foundation.storeView);this.knowledge=new KnowledgeGovernance(foundation);}verifyClaim(i:Omit<Claim,"id"|"fingerprint">&{id?:string},c:GovernanceContext):ClaimVerificationResult{const r=this.claims.verify(i);invariant(c.reason.trim().length>0,"V8_GOVERNANCE_REASON_REQUIRED","Governance actions require an audit reason.");invariant(c.actor.role==="VERIFIER","V8_GOVERNANCE_VERIFIER_ROLE_REQUIRED","Claim verification requires VERIFIER.");if(r.verdict==="VERIFIED")this.foundation.createClaim(r.claim,c.actor,c.reason);return immutable(r);}approveKnowledge(i:Omit<Knowledge,"id"|"fingerprint">&{id?:string},c:GovernanceContext):KnowledgeApprovalResult{return this.knowledge.approve(i,c);}}
